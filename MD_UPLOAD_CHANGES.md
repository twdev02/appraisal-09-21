# MD 선택 업로드 변경 안내 — 2026-09-21

## 사용 방법

1. Step 1에서 DUE를 설정하고 PDF를 등록합니다. 이때 분석은 시작되지 않습니다.
2. 각 논문 카드의 `Attach MD (optional)`에서 해당 논문의 MD를 선택합니다.
3. `Start Analysis`를 누르면 대기 중인 논문을 순서대로 분석합니다.
4. MD가 없으면 기존 PDF 분석 경로를 사용합니다. 자동 MD 생성은 실행하지 않습니다.
5. MD가 있으면 기존 서버의 MD 대조 기능에 전달합니다.
6. 분석 후 MD를 교체/삭제하면 재분석 안내가 표시됩니다. `Reanalyze` 또는 `Start Analysis`로 결과를 갱신하세요. 갱신 전 결과는 이전 입력 기준이며 일괄 Word 내보내기에서 제외됩니다.

MD는 논문별로 연결됩니다. 파일명 자동 매칭은 제거했습니다. 파일명은 달라도 되지만 동일 논문인지 사용자가 확인해야 합니다. 빈 파일과 잘못된 확장자는 거부하며, 분석 중에는 첨부 변경이 잠깁니다. 파일은 브라우저 세션에서만 유지됩니다.

## 수정 범위

- src/App.tsx: PDF 등록/분석 시작 분리, MD 연결 및 재분석 상태, 요청 전달
- src/components/Step1Setup.tsx: 논문 카드별 MD 첨부/교체/삭제, 분석 시작 버튼
- src/types.ts: MD 변경 후 재분석 필요 상태
- README.md: 새 사용 흐름
- .gitignore: 의존성, 빌드 결과, 비밀 설정 제외
- package-lock.json: 검증에 사용한 의존성 버전 고정

서버 추출·대조 규칙, 평가 기준, 점수 계산, Word 생성 로직은 변경하지 않았습니다. 이 최신 원본 ZIP에는 이미 자동 변환 및 별도 Markdown Lab이 제거되어 있어 그 상태를 유지했습니다.

## 검증

- TypeScript 검사 통과 (`npm run lint`)
- 프런트엔드/서버 빌드 통과 (`npm run build`)
- 카드 렌더링 검사 통과: PDF 전용 입력, 선택 MD 첨부, 교체/삭제, 대기 상태, 재분석 안내, 분석 중 잠금
- 기존 서버 및 Word 생성 코드 변경 없음
- 실행 환경의 서버 시작 권한 오류로 실제 브라우저 클릭·Gemini 요청 검증은 수행하지 못했습니다.
- 외부 NotebookLM MD 샘플은 제공되지 않아 실제 표/페이지 구조 호환성은 미검증입니다.

## GitHub 반영

이 ZIP은 전체 소스입니다. 기존 저장소의 루트에 압축 내부 `Appraisal-0918` 폴더의 내용을 반영하세요. 동일 이름의 폴더를 저장소 안에 한 단계 더 만들지 마세요.
기존 `.git`과 `.env`는 유지하세요. ZIP에는 `.git`, 실제 비밀 설정, node_modules, dist가 포함되지 않습니다.
먼저 별도 브랜치에서 변경을 검토하고 커밋하는 것을 권장합니다. GitHub 직접 반영은 계정 연결 전까지 수행하지 않았습니다.
